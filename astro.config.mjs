// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://jmj-cloud.github.io',
  base: '/maren-stokes.github.io',
  output: 'static',
});
