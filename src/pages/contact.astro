---
import Layout from '../components/Layout.astro';
import SectionHeading from '../components/SectionHeading.astro';
---

<Layout
  title="Contact"
  description="Get in touch with Maren Stokes for collaboration, opportunities, or conversation."
>
  <div class="contact-page">
    <SectionHeading
      title="Let's Connect"
      subtitle="I'd love to hear from you"
      centered={true}
    />

    <div class="contact-content">
      <div class="contact-intro">
        <p class="intro-text">
          Feel free to reach out — I love connecting, collaborating, and learning from new people.
          Whether you want to discuss STEM projects, leadership opportunities, or just have a
          conversation about math, physics, or technology, I'm always happy to chat.
        </p>
      </div>

      <div class="contact-methods">
        <div class="contact-card">
          <h3>Email</h3>
          <p class="contact-label">Preferred method of contact</p>
          <p class="contact-value">[Email to be added]</p>
          <p class="contact-note">
            Feel free to email me about collaboration opportunities, questions about my work,
            or just to connect.
          </p>
        </div>

        <div class="contact-card">
          <h3>LinkedIn</h3>
          <p class="contact-label">Professional network</p>
          <p class="contact-value">[LinkedIn profile to be added]</p>
          <p class="contact-note">
            Connect with me on LinkedIn to stay updated on my academic and professional journey.
          </p>
        </div>
      </div>

      <div class="cta-section">
        <h3>What I'm Looking For</h3>
        <div class="interests-list">
          <div class="interest-item">
            <div class="interest-text">
              <strong>College Opportunities</strong>
              <p>Exploring programs in mathematics, physics, computer science, and machine learning</p>
            </div>
          </div>

          <div class="interest-item">
            <div class="interest-text">
              <strong>Research Collaboration</strong>
              <p>Interested in research projects related to ML, applied math, or computational physics</p>
            </div>
          </div>

          <div class="interest-item">
            <div class="interest-text">
              <strong>Learning & Mentorship</strong>
              <p>Always eager to learn from experienced professionals and academics in STEM fields</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  .contact-page {
    max-width: 800px;
    margin: 0 auto;
  }

  .contact-content {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-3xl);
  }

  .contact-intro {
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .intro-text {
    font-size: var(--font-size-lg);
    line-height: 1.7;
    color: var(--color-text);
  }

  .contact-methods {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-xl);
    margin-top: var(--spacing-xl);
  }

  .contact-card {
    background-color: var(--color-white);
    padding: var(--spacing-2xl);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-sm);
    border: 1px solid rgba(74, 74, 74, 0.08);
    text-align: center;
    transition: all var(--transition-base);
  }

  .contact-card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
    border-color: var(--color-accent);
  }

  .contact-icon {
    font-size: var(--font-size-4xl);
    line-height: 1;
    margin-bottom: var(--spacing-md);
  }

  .contact-card h3 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: 700;
    color: var(--color-text-dark);
    margin-bottom: var(--spacing-sm);
  }

  .contact-label {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    margin-bottom: var(--spacing-sm);
  }

  .contact-value {
    font-size: var(--font-size-base);
    font-weight: 600;
    color: var(--color-accent);
    margin-bottom: var(--spacing-md);
  }

  .contact-note {
    font-size: var(--font-size-sm);
    line-height: 1.6;
    color: var(--color-text);
    margin: 0;
  }

  .cta-section {
    background: linear-gradient(135deg, var(--color-bg-alt) 0%, var(--color-white) 100%);
    padding: var(--spacing-2xl);
    border-radius: var(--radius-xl);
    border: 1px solid rgba(74, 74, 74, 0.08);
  }

  .cta-section h3 {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: 700;
    color: var(--color-text-dark);
    text-align: center;
    margin-bottom: var(--spacing-xl);
  }

  .interests-list {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
  }

  .interest-item {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-md);
    padding: var(--spacing-lg);
    background-color: var(--color-white);
    border-radius: var(--radius-lg);
    transition: all var(--transition-base);
  }

  .interest-item:hover {
    transform: translateX(4px);
    box-shadow: var(--shadow-sm);
  }

  .interest-icon {
    font-size: var(--font-size-2xl);
    line-height: 1;
    flex-shrink: 0;
  }

  .interest-text {
    flex: 1;
  }

  .interest-text strong {
    display: block;
    font-family: var(--font-heading);
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--color-text-dark);
    margin-bottom: var(--spacing-xs);
  }

  .interest-text p {
    font-size: var(--font-size-base);
    line-height: 1.6;
    color: var(--color-text);
    margin: 0;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .intro-text {
      font-size: var(--font-size-base);
    }

    .contact-methods {
      grid-template-columns: 1fr;
      gap: var(--spacing-lg);
    }

    .contact-card {
      padding: var(--spacing-xl);
    }

    .contact-icon {
      font-size: var(--font-size-3xl);
    }

    .cta-section {
      padding: var(--spacing-xl);
    }

    .cta-section h3 {
      font-size: var(--font-size-xl);
    }

    .interest-item {
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: var(--spacing-md);
    }

    .interest-icon {
      font-size: var(--font-size-xl);
    }
  }
</style>
